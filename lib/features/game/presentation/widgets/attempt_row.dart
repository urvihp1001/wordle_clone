import 'package:flutter/material.dart';

class AttemptRow extends StatelessWidget {
  const AttemptRow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}